#calcolo indice di massa corporea

height = float(input("Inserisci l'altezza in mt: "))
weight = float(input("Inserisci il peso in kg: "))

BMI = (weight)  / (height * height)

print("L'indice di massa corporea è {}.".format(BMI))

